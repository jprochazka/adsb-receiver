<?php
    class settings {

        // Database Settings
        const db_driver = 'xml';
        const db_database = '';
        const db_username = '';
        const db_password = '';
        const db_host = '';
        const db_prefix = 'adsb_';

        // Security Settings
        const sec_length = 6;

        // PDO Settings
        const pdo_debug = FALSE;
    }

?>
